// use db_test_config::{Config, get_config_path};
// use db_test_model::generate_data;

fn main() -> anyhow::Result<()> {
    // let config = Config::try_read()?;
    // generate_data(config.data.into_iter())?;
    Ok(())
}
